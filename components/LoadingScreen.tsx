import { ActivityIndicator } from "react-native";

const LoadingScreen = () => (
  <ActivityIndicator
    size="large"
    color="#0000FF"
    className="mt-10 self-center"
  />
);

export default LoadingScreen;
